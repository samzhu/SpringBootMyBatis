general:
  artifacts:
    - "target/SpringBootMyBatis-*.jar"

machine:
  java:
    version: oraclejdk8
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t java:8 .


